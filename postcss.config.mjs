const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      fontFamily: {
        sf: ["-apple-system", "BlinkMacSystemFont", "SF Pro", "Helvetica Neue", "Arial", "sans-serif"],
      },
    },
  },
};

export default config;